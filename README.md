# homework-downloader

## Dependencies
```sh
brew install wget
npm install
```

## Usage
Download a .csv copy of the homework submissions document.

Put it at `submissions.csv` in this directory.

```sh
node download-homework-1.js
```

And browse the `submission-content` directory.
